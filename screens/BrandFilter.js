import React from "react";
import { Text, View, StyleSheet } from "react-native";

const BrandFilter = ({ params }) => (
	<View>
		<Text>BrandFilter</Text>
	</View>
);

const style = StyleSheet.create({});

export default BrandFilter;
